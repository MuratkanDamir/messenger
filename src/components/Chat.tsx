import { useAppSelector } from "hooks/hooks";

const style: React.CSSProperties = {
    width:'70vw', 
    minWidth:'400px',
    backgroundColor:'violet'
}
const Chat: React.FC = () => {
    return (
        <div style={style}>
            Chat
        </div>
    );
  }
  
  export default Chat;
  